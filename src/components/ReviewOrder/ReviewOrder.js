import React from 'react';
import img from '../../images/giphy.gif'

const ReviewOrder = () => {
    return (
        <div>
            <img src={img} alt=''/>
        </div>
    );
};

export default ReviewOrder;